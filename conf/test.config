// Tiny real test inputs. Combine with an engine profile, e.g. `-profile test,podman`.
// Run scripts/fetch_test_data.sh first to populate test_data/.
params {
    input = "${projectDir}/assets/samplesheet_test.csv"
}
